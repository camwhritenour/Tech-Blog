{{#each posts as |post| }}
<div class="card">
  <div class="card card-header">
    <h2 class=""> {{post.title}} </h2>
    <h5>{{post.creator_name}} {{post.date_created}}</h5>
  </div>
  <div>
    <p class="card-body">{{post.content}}</p>
  </div>
</div>
{{/each}}